# Live-planner stack (install on Colab / local GPU box)
transformers>=4.45
accelerate>=0.34
torch>=2.2
sentencepiece
safetensors

# High-throughput option
# vllm>=0.6

# Service layer (optional)
fastapi
uvicorn[standard]

# Data / metrics
pandas
numpy
matplotlib

# Quantisation — REQUIRED for load_in_4bit / for_t4() on a 16 GB T4.
# Lets 7B-class models (Mistral-7B, Qwen2.5-7B) run without CPU/disk
# offload. Linux + CUDA only.
bitsandbytes>=0.43
