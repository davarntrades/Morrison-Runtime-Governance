name: cma-priority-followup

on:
  push:
    branches: [experiment/cma-falsification-20260923]
    paths:
      - .github/workflows/cma-priority-followup.yml
      - experiments/cma_priority_followup.py
  workflow_dispatch:

permissions:
  contents: read

jobs:
  live:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          path: campaign
          persist-credentials: false
      - uses: actions/checkout@v4
        with:
          repository: davarntrades/launch-your-agent
          ref: 1f3db3c37d7b394185df0e5a5a12efcffe1ac5d6
          path: source
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Verify source and reuse live synthetic resources
        run: |
          test "$(git -C source rev-parse HEAD)" = 1f3db3c37d7b394185df0e5a5a12efcffe1ac5d6
          printf 'AGENT_ID=agent_01Dfx9yFhSrmhZHSkLry65Qu\nAGENT_VERSION=1\nENV_ID=env_0152rdQFEnyBZG9TBK2YVe3Y\n' > source/governance-agent/IDS.env
          git -C campaign rev-parse HEAD > source/followup-campaign-commit.txt
          git -C source rev-parse HEAD > source/followup-prototype-commit.txt
          sha256sum campaign/experiments/cma_priority_followup.py > source/followup-script.sha256
      - name: Corrected live priority probes
        working-directory: source
        continue-on-error: true
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python3 ../campaign/experiments/cma_priority_followup.py
      - name: Reject credential-like material
        if: always()
        id: secret_scan
        run: |
          if grep -rIlE 'sk-ant-|gh[pousr]_|Bearer [A-Za-z0-9._-]{20,}' source/experiments 2>/dev/null; then
            echo 'Credential-like material found; refusing artifact'
            exit 1
          fi
      - name: Upload raw evidence
        if: always() && steps.secret_scan.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: cma-priority-followup-${{ github.run_id }}
          path: |
            source/experiments/evidence
            source/followup-campaign-commit.txt
            source/followup-prototype-commit.txt
            source/followup-script.sha256
          retention-days: 14
